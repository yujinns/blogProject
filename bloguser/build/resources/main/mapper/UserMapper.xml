<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="yujinns.bloguser.mapper.UserMapper">

    <insert id="insertUser" parameterType="yujinns.bloguser.dto.UserDto">
        insert into yujinns_user (id, user_id, password, nickname, email) values (user_seq.nextval, #{userId}, #{password}, #{nickName}, #{email})
    </insert>
</mapper>